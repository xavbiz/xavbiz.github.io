version: "3"

services:
    node:
      container_name: carbon.node
      build:
        context: ./
        dockerfile: Dockerfile
      volumes:
        - ./site:/home/node/app
      ports:
        - 3000:3000
